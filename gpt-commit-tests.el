;;; gpt-commit-tests.el --- Tests for gpt-commit.el -*- lexical-binding: t; -*-

;;; Commentary:

;; Tests for gpt-commit.el

;;; Code:

(require 'gpt-commit)
(require 'ert)

(provide 'gpt-commit-tests)
;;; gpt-commit-tests.el ends here
